
import { answerQuality } from 'qa-message-board-ember/helpers/answer-quality';
import { module, test } from 'qunit';

module('Unit | Helper | answer quality');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerQuality([42]);
  assert.ok(result);
});

