
import { bestAnswer } from 'qa-message-board-ember/helpers/best-answer';
import { module, test } from 'qunit';

module('Unit | Helper | best answer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bestAnswer([42]);
  assert.ok(result);
});

