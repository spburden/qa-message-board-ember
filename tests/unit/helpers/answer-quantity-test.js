
import { answerQuantity } from 'qa-message-board-ember/helpers/answer-quantity';
import { module, test } from 'qunit';

module('Unit | Helper | answer quantity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerQuantity([42]);
  assert.ok(result);
});

