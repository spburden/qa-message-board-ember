
import { mostPopular } from 'qa-message-board-ember/helpers/most-popular';
import { module, test } from 'qunit';

module('Unit | Helper | most popular');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mostPopular([42]);
  assert.ok(result);
});

